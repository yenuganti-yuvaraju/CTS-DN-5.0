USE OnlineRetailDB;
GO

CREATE FUNCTION dbo.GetPrice
(
    @ProductID INT
)
RETURNS DECIMAL(10,2)
AS
BEGIN
    DECLARE @Price DECIMAL(10,2);

    SELECT @Price = Price
    FROM Products_Function
    WHERE ProductID = @ProductID;

    RETURN @Price;
END;
GO